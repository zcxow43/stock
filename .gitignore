# Node dependencies (docs mermaid renderer — pulls in Chromium via puppeteer)
node_modules/

# Maven build output
target/

# Detached dev-server state written by /start (PIDs + logs); never committed
.run/
